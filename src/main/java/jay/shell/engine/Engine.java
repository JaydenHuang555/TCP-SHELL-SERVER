package jay.shell.engine;

public class Engine {

     /*
     @chris
     must be parsed into an array (without whitespace)
     (
        s = "this is an test\n"
        arg = ["this", "is", "an", "test"]
     )
      */
    public void handle(String s){

    }

}
